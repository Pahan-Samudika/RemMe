<resources>
    <!-- Define a style for your date and time input fields -->
    <style name="DateTimeEditTextStyle" parent="Widget.AppCompat.EditText">
        <item name="android:background">@drawable/field_border</item>
        <item name="android:fontFamily">@font/poppins</item>
        <item name="android:textColor">@color/white</item>
        <item name="android:textColorHint">@color/grey</item>
        <item name="android:padding">12dp</item>
        <item name="android:textSize">18sp</item>
    </style>

    <style name="MyDatePicker" >
        <!-- Text color -->
        <item name="android:textColorPrimary">@color/white</item>
        <!-- Divider color -->
        <item name="colorControlNormal">@color/greenYellow</item>
    </style>

</resources>
